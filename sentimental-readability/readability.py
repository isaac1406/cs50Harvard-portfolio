from cs50 import get_string

numlet = 0
numwor = 0
numsen = 0

def main():
    text = get_string("Text: ")

def counter(text):
    numlet = len
    for i in range(len(text)):
        if (text[i] = " ")
